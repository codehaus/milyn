# Update the deployment lib to deploy directly into container.
deploy.lib=../build
smooks.version=0.6-M1

# Shouldn't need to modify the properties below.
build.root=..
build.lib=${build.root}/lib
build.src=${build.root}/src
build.classes=${build.root}/classes
build.docs=${build.root}/docs
mock.src=${build.root}/test/mock/src
mock.classes=${build.root}/test/mock/classes
test.root=${build.root}/test/junit
test.lib=${test.root}/lib

env.debug=true
