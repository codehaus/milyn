The Maven2 POM file located in this folder (get-latest.xml) can be used to pull the
latest Smooks libs from the baven repo.

Usage: "mvn -f get-latest.xml clean package"

The libs are assembled and droped in the "./target/lib" folder.